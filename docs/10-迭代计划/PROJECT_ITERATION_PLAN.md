# Labex 项目迭代修复计划

> 基于 2026-06-11 全面代码诊断生成
> 预计总工期：6-8 周

---

## 📋 问题总览

| 类别 | 问题数量 | 严重程度 |
|------|----------|----------|
| 事务缺失 | 10+ 处 | 🔴 严重 |
| 未完成功能 | 4 处 | 🔴 严重 |
| 代码重复 | 15+ 处 | 🟡 中等 |
| 硬编码问题 | 8 处 | 🟡 中等 |
| 错误处理 | 70+ 处 | 🟡 中等 |
| 测试缺失 | 85+ 个模块 | 🟡 中等 |

---

## 🚀 迭代计划

### 第一阶段：紧急修复（第 1-2 周）

**目标：修复数据一致性风险和未完成功能**

#### 1.1 事务管理修复（优先级：P0）

| 任务 | 文件 | 修改内容 | 预计工时 |
|------|------|----------|----------|
| 考试提交事务 | `StudentExamController.java` | 给 `submitExam()` 添加 `@Transactional` | 0.5h |
| 作业提交事务 | `StudentHomeworkController.java` | 给 `submitHomework()` 添加 `@Transactional` | 0.5h |
| 批改提交事务 | `TeacherGradingController.java` | 给 `submitAllGrades()` 和 `gradeQuestion()` 添加 `@Transactional` | 0.5h |
| 训练提交事务 | `StudentTrainingController.java` | 给 `submit()` 添加 `@Transactional` | 0.5h |
| 训练集删除事务 | `TrainingSetController.java` | 给 `delete()` 添加 `@Transactional` | 0.5h |
| 试卷操作事务 | `PaperController.java` | 给试卷创建+题目关联添加 `@Transactional` | 0.5h |
| Diff 操作事务 | `DiffService.java` | 给 `stage()` 和 `apply()` 添加 `@Transactional` | 1h |

**验收标准：** 所有多表写入操作都有事务保护，中间失败能正确回滚

#### 1.2 未完成功能实现（优先级：P0）

| 任务 | 文件 | 修改内容 | 预计工时 |
|------|------|----------|----------|
| 成绩导出功能 | `ScoreController.java` | 实现 Excel 导出（使用 Apache POI） | 4h |
| CurrentUser 修复 | `CurrentUser.java` | 修复 `getUserId()` 返回 null 的问题 | 1h |
| 作业简答批改 | 新增教师批改页面 | 实现作业简答题的教师手动批改入口 | 8h |
| 云空间审查数据 | `CloudWorkspace.vue` | 移除硬编码假数据，接入真实 API | 4h |

**验收标准：** 所有标记为"待实现"的功能都能正常使用

---

### 第二阶段：代码质量提升（第 3-4 周）

**目标：消除硬编码、改善错误处理**

#### 2.1 硬编码修复（优先级：P1）

| 任务 | 文件 | 修改内容 | 预计工时 |
|------|------|----------|----------|
| 上传路径配置化 | `RagConfig.java` | 将 `D:/workfordasan/uploads` 改为配置项 | 1h |
| 项目路径配置化 | `StudentProjectServiceImpl.java` | 将 `D:/workfordasan/student_projects` 改为配置项 | 1h |
| 移除硬编码密码 | `PasswordEncoderUtil.java` | 移除硬编码的 `"root"` 密码 | 0.5h |
| 魔法数字常量化 | 多个文件 | 将魔法数字提取为常量并添加注释 | 2h |
| 模板外置 | `StudentProjectServiceImpl.java` | 将 5 个项目模板提取为资源文件 | 2h |

**验收标准：** 所有路径、地址都通过配置文件管理，无硬编码密码

#### 2.2 错误处理改善（优先级：P1）

| 任务 | 文件 | 修改内容 | 预计工时 |
|------|------|----------|----------|
| 后端空 catch 块 | 6 处 | 至少添加日志记录 | 1h |
| 前端空 catch 块 | 70+ 处 | 添加错误提示或日志 | 4h |
| 关键错误传播 | 11 处 | 将仅 log.warn 改为适当的错误传播 | 3h |
| 移除调试日志 | 2 个文件 | 移除 11 个 `[DEBUG]` console.log | 0.5h |

**验收标准：** 关键操作失败时用户能看到错误提示，调试日志全部移除

---

### 第三阶段：代码复用重构（第 5-6 周）

**目标：消除重复代码，提升可维护性**

#### 3.1 前端 API 工厂（优先级：P1）

| 任务 | 文件 | 修改内容 | 预计工时 |
|------|------|----------|----------|
| 创建 API 工厂 | `api/index.js` | 实现 `createCrudApi()` 工厂函数 | 2h |
| 重构 teacherApi | `api/index.js` | 8 个子对象使用工厂函数 | 2h |
| 重构 adminApi | `api/index.js` | 3 个子对象使用工厂函数 | 1h |
| 修复重复定义 | `studentApi` | 删除 `lectures()`/`getLecture()` 的重复定义 | 0.5h |

**验收标准：** API 定义代码减少 300+ 行，功能不变

#### 3.2 前端通用 Composable（优先级：P1）

| 任务 | 文件 | 修改内容 | 预计工时 |
|------|------|----------|----------|
| 创建 useCrudPage | `composables/useCrudPage.js` | 抽取通用列表页逻辑 | 4h |
| 创建 usePagination | `composables/usePagination.js` | 抽取分页逻辑 | 2h |
| 重构教师列表页 | 6+ 个 Vue 文件 | 使用 composable 替换重复逻辑 | 8h |
| 重构管理员页面 | 2 个 Vue 文件 | 与教师端共享组件 | 4h |

**验收标准：** 列表页代码减少 600+ 行，新增页面只需配置即可

#### 3.3 前端样式统一（优先级：P2）

| 任务 | 文件 | 修改内容 | 预计工时 |
|------|------|----------|----------|
| 创建全局样式 | `styles/page-layout.scss` | 抽取通用页面布局样式 | 2h |
| 创建对话框主题 | `styles/dialog-theme.scss` | 统一对话框样式 | 1h |
| 重构页面样式 | 6+ 个 Vue 文件 | 使用全局样式替换重复 CSS | 3h |

**验收标准：** 样式代码减少 200+ 行，主题统一

#### 3.4 后端代码复用（优先级：P1）

| 任务 | 文件 | 修改内容 | 预计工时 |
|------|------|----------|----------|
| 统一 getCurrentTeacherId | 新增 `TeacherContext.java` | 抽取为可注入的组件 | 2h |
| 统一 PageResult 构建 | 7 个 Controller | 使用 `PageResult.success()` 静态方法 | 1h |
| 抽取文件工具类 | 新增 `FileUtil.java` | 统一文件上传/下载/预览逻辑 | 4h |
| 统一认证获取 | `CurrentUser.java` | 增加 `getCurrentTeacher()` 和 `getCurrentStudent()` | 2h |

**验收标准：** 后端重复代码减少 700+ 行

---

### 第四阶段：测试补全（第 7-8 周）

**目标：提升测试覆盖率到 30%+**

#### 4.1 后端核心测试（优先级：P2）

| 任务 | 文件 | 测试内容 | 预计工时 |
|------|------|----------|----------|
| 考试流程测试 | `StudentExamControllerTest.java` | 提交、评分、查看成绩 | 4h |
| 作业流程测试 | `StudentHomeworkControllerTest.java` | 提交、评分、文件上传 | 4h |
| 训练流程测试 | `StudentTrainingControllerTest.java` | 练习、提交、查看记录 | 3h |
| 批改流程测试 | `TeacherGradingControllerTest.java` | 批改、批量提交、完成检查 | 3h |
| 班级管理测试 | `ClazzControllerTest.java` | CRUD、分页、搜索 | 2h |
| 学生管理测试 | `TeacherStudentControllerTest.java` | CRUD、导入、状态切换 | 2h |

**验收标准：** 核心业务流程有测试覆盖，能正常运行

#### 4.2 前端组件测试（优先级：P2）

| 任务 | 文件 | 测试内容 | 预计工时 |
|------|------|----------|----------|
| 登录组件测试 | `Login.test.js` | 登录、注册、表单验证 | 2h |
| 通用列表页测试 | `CrudListPage.test.js` | 分页、搜索、删除 | 2h |
| Monaco 编辑器测试 | `MonacoEditor.test.js` | 加载、编辑、语言切换 | 2h |

**验收标准：** 核心组件有测试覆盖

---

## 📊 迭代里程碑

| 阶段 | 时间 | 里程碑 | 验收标准 |
|------|------|--------|----------|
| 第一阶段 | 第 1-2 周 | 数据一致性保障 | 所有多表操作有事务，未完成功能实现 |
| 第二阶段 | 第 3-4 周 | 代码质量提升 | 无硬编码，错误处理完善 |
| 第三阶段 | 第 5-6 周 | 代码复用重构 | 重复代码减少 80%+ |
| 第四阶段 | 第 7-8 周 | 测试补全 | 核心流程测试覆盖 30%+ |

---

## 📈 预期收益

| 指标 | 当前 | 目标 |
|------|------|------|
| 事务覆盖 | 3 处 | 13+ 处 |
| 未完成功能 | 4 处 | 0 处 |
| 硬编码问题 | 8 处 | 0 处 |
| 前端重复代码 | ~1800 行 | ~200 行 |
| 后端重复代码 | ~700 行 | ~100 行 |
| 后端测试覆盖 | 3.2% | 30%+ |
| 前端测试覆盖 | 1.8% | 15%+ |

---

## 🔧 技术债务清单

### 需要后续迭代处理的问题

| 问题 | 优先级 | 说明 |
|------|--------|------|
| RagChat 死代码清理 | P3 | `v-if="false"` 模板块和无用 CSS |
| ClazzService 冗余方法 | P3 | `findByNo`/`removeByNo` 与内置方法重复 |
| 分页组件统一 | P3 | 已有 `Pagination.vue` 但未使用 |
| 代码执行语言扩展 | P3 | `ExecuteCodeTool` 仅支持 Python/Node |
| 向量索引降级告警 | P3 | Neo4j 向量索引失败时应有更明显的告警 |

---

## 📝 注意事项

1. **每个阶段结束后进行代码审查**，确保修改不引入新问题
2. **事务修复需要充分测试**，模拟各种失败场景验证回滚
3. **代码复用重构保持向后兼容**，不要修改 API 接口
4. **测试补全优先覆盖核心业务流程**，而非追求覆盖率数字
5. **每次迭代后更新本文档**，标记已完成的任务

---

## 📅 检查点

- [ ] 第 2 周末：事务管理修复完成，未完成功能实现
- [ ] 第 4 周末：硬编码和错误处理修复完成
- [ ] 第 6 周末：代码复用重构完成
- [ ] 第 8 周末：测试补全完成，项目达到发布标准

---

*本计划基于 2026-06-11 代码诊断报告生成，执行过程中可根据实际情况调整。*
